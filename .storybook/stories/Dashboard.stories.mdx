import {
  Story,
  Heading,
} from '@storybook/addon-docs/blocks';
import { ChartComponentUsage, ChartStoriesPerComponent } from '../../dist/blocks';

<Meta
  title="About/Dashboard"
/>

<Heading>Stories per component</Heading>
Displays how many stories are written per component (the more the merrier)
<ChartStoriesPerComponent
  height='500px'
  options={{
    sliceVisibilityThreshold: 0.04, 
    pieSliceText: 'value',
    // is3D: true,
    // pieHole: 0.7,
    slices: {
      0: { offset: 0.1 },
    }  
  }}
/>

<Heading>Components usage</Heading>
How many times a component is used, click to see by which components
<br /><br />
<ChartComponentUsage
  height='500px'
  options={{
    minColor: '#009688',
    midColor: '#f7f7f7',
    maxColor: '#ee8100',
  }}
/>