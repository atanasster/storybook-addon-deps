import {
  Story,
} from '@storybook/addon-docs/blocks';
import { SectionTitle, ChartComponentUsage, ChartStoriesPerComponent } from '../../dist/blocks';

<Meta
  title="About|Dashboard"
/>

<SectionTitle>Stories per component</SectionTitle>
Displays how many stories are written per component (the more the merrier)
<ChartStoriesPerComponent
  height='500px'
  options={{
    sliceVisibilityThreshold: 0.04, 
    pieSliceText: 'value',
    // is3D: true,
    // pieHole: 0.7,
    slices: {
      0: { offset: 0.1 },
    }  
  }}
/>

<SectionTitle>Components usage</SectionTitle>
How many times a component is used, click to see by which components
<br /><br />
<ChartComponentUsage
  height='500px'
  options={{
    minColor: '#009688',
    midColor: '#f7f7f7',
    maxColor: '#ee8100',
  }}
/>